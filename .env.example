# Copy to .env and fill in. Never commit the real .env.

GOOGLE_MAPS_API_KEY=        # if source=google (Places API New + Geocoding enabled, billing on)
APIFY_TOKEN=                # if source=apify
NOTION_TOKEN=
NOTION_LEADS_DB_ID=
NOTION_ROUTES_DB_ID=
ANTHROPIC_API_KEY=          # optional, for --llm-uncertain
